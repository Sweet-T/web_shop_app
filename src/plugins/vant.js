import Vue from 'vue';
import {
    Button,
    NavBar,
    Tabbar,
    TabbarItem,
    Swipe,
    SwipeItem,
    Lazyload,
    Grid, 
    GridItem,
    Image,
    Col, 
    Row
} from 'vant';

import 'vant/lib/index.css';

Vue
.use(Button)
.use(NavBar)
.use(Tabbar)
.use(TabbarItem)
.use(Swipe)
.use(SwipeItem)
.use(Lazyload)
.use(Grid)
.use(GridItem)
.use(Image)
.use(Col)
.use(Row)